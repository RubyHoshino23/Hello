# Copy to `.env` or `.env.local` (not committed). Vite exposes only VITE_* to the client.

# Set to false for a handoff build without the floating Live Chat widget
VITE_ENABLE_LIVE_CHAT=true

# Set to false to disable ?agent=1 agent console (optional)
VITE_ENABLE_AGENT_CONSOLE=true
